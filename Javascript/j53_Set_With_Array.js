// Give an Array with repeated elements
// and
// Get a Set with non-repeated elements

const array = [1, 2, 3, 2, 5, 3, 2, 4, 6, 5, 5, 2, 1, 1, 3];
const newSet = new Set(array);
console.log(newSet);